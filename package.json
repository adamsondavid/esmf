{
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "turbo run dev",
    "check-format": "prettier --ignore-path .gitignore --ignore-path .prettierignore --check .",
    "check-types": "turbo run check-types",
    "build": "turbo run build"
  },
  "packageManager": "pnpm@10.28.1",
  "devDependencies": {
    "@changesets/cli": "^2.29.8",
    "prettier": "^3.8.1",
    "turbo": "^2.7.5"
  }
}
